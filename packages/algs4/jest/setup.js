const canvasContainer = document.createElement('div');
canvasContainer.id = 'CanvasContainer';

document.body.appendChild(canvasContainer);

// Show electron window for debug
// require('electron').remote.getCurrentWindow().show();
